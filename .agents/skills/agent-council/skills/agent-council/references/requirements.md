# Requirements

- OpenCode installed and configured. The council uses OpenCode's native `Task` tool for spawning subagents.
- No external CLIs, Node.js, or additional dependencies are required.
- Members can optionally use different models via the `model` field in `council.config.yaml`. Ensure those models are configured in OpenCode (`opencode providers`).
