# Host UI

OpenCode does not require a separate host UI integration. The council workflow runs natively through OpenCode's `Task` tool. Progress is visible inline as each subagent completes.

The shell scripts under `scripts/` (legacy) include Claude Code / Codex CLI checklist integration. Those are preserved for reference but not needed with OpenCode.
